#pragma once

int getHeartRate() {
	int rate;
	int hiber_min;
	if (hiber_min == 5) {
		return rate;
	}
}

int playSound(int BPM) {

	if (BPM == 150 || BPM == 130 || BPM == 170) {
		printf("正在播放BPM为%d的音乐", BPM);
		return 0;
	}
	else {
		printf("播放失败或未找到音乐");
		return -1;
	}

}